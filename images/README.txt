Place the uploaded logo image here as `logo.png`.

Recommended: save a high-resolution PNG (or SVG) named `logo.png`.
Then reload the site — the header will use `images/logo.png` automatically.

If you want me to convert the uploaded PDF to PNG or SVG, tell me and I'll do it.